apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: cache
    app.openshift.io/runtime: redis
spec:
  selector: { app: redis }
  ports:
    - port: 6379
      targetPort: 6379
