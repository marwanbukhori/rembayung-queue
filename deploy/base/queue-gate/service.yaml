apiVersion: v1
kind: Service
metadata:
  name: queue-gate
  labels:
    app.kubernetes.io/name: queue-gate
    app.kubernetes.io/component: gateway
    app.openshift.io/runtime: spring-boot
spec:
  selector: { app: queue-gate }
  ports:
    - name: http
      port: 8080
      targetPort: 8080
